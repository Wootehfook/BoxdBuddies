# npm configuration for BoxdBuddies
# These settings manage known vulnerabilities in dev-only dependencies

# Allow installation despite peer dependency conflicts in the ESLint toolchain
# (see docs/npm-audit-decision.md for detailed analysis)
legacy-peer-deps=true

# Note on vulnerabilities:
# - 11 dev-only vulnerabilities exist in ESLint/TypeScript-ESLint ecosystem
# - Impact: None (these tools don't run in production)
# - Fix: Requires upstream ecosystem changes (in progress)
# - Mitigations:
#   * In Docker/CI: use `npm ci --omit=dev` for prod deployments
#   * For audits: `npm audit --omit=dev` to check runtime-only code
#   * See docs/npm-audit-decision.md for full analysis


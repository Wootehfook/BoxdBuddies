# CodeQL Configuration for BoxdBuddies Web App
# AI Generated: GitHub Copilot - 2025-08-17

name: "BoxdBuddies Web App CodeQL Configuration"

# Restrict analysis to our JS/TS source roots (default setup reads this file automatically)
paths:
  - src/
  - functions/

# Global paths to exclude from all analysis
paths-ignore:
  - node_modules/
  - dist/
  - target/
  - .git/
  - "**/*.test.ts"
  - "**/*.test.js"
  - "**/*.spec.ts"
  - "**/*.spec.js"
  - "**/*.d.ts"

# Keep default queries enabled and add extended security+quality suites
disable-default-queries: false
queries:
  - uses: security-extended
  - uses: security-and-quality
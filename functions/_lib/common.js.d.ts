// Neutralized duplicate declarations for runtime import '../../_lib/common.js'.
// Canonical, authoritative declarations are in `functions/_types/ambient-common.d.ts` and `functions/_types/common-modules.d.ts`.
// This file is intentionally left minimal to avoid duplicate-symbol lint/type issues.

export declare function parseGenresToNames(
  genres: unknown
): string[] | undefined;
